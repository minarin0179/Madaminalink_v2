version: "3"
services:
  mongo:
    image: mongo
    container_name: mongodb
    restart: always
    expose:
      - "27017"
    volumes:
      - ./.db:/data/db

  bot:
    build: .
    container_name: madaminalink
    depends_on:
      - mongo
